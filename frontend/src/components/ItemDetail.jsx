import React from 'react'

const ItemDetail = ({ data }) => {
    const { id, nombre, precio, categoria, descripcion, imagen } = data
  return (
    <div>ItemDetail</div>
  )
}

export default ItemDetail