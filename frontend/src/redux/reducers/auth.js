import { types } from "../types";

const initalState = {
    data: [],
}

export const authReducer = (state = initalState, action) => {
    switch (action.type) {

        default:
            return state;
    }
}